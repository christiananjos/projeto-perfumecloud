{
  "name": "perfumecloud-pro-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "PerfumeCloud Pro - frontend Vue 3 (CDN, sem build). Este package.json existe só para rodar os testes unitários (Vitest); o deploy continua estático, sem bundler.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
